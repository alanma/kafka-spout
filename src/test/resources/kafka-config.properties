# kafka configuration to connect to zookeeper
zookeeper.connect = 192.168.86.5:2181
# consumer group Id
group.id=max-group
# kafka configuration for consumer operation (-1 is default, indicates indefinite wait for data;
# otherwise, ConsumerTimeoutException will be thrown when specified timeout has been reached without any incoming data)
consumer.timeout.ms = -1
